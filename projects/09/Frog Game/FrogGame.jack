class FrogGame {
  function void run() {
    var boolean running;
    var char keyPressed;

    let running = true;

    // The game loop. In this loop we will:
    //  1. Check for user input
    //  2. Move 
    //  3. Resolve collisions
    //  4. Draw graphics
    while (running) {
      while (keyPressed = 0) {
        let key = Keyboard.keyPressed();
      }
    }

    return;
  }
}